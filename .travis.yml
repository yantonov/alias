language: rust

rust:
  - stable

os:
  - linux
  - osx

script:
  - cargo build --verbose --workspace --release
  - cargo test --verbose --all

before_deploy: "mv target/release/alias target/release/alias_$TRAVIS_OS_NAME"

deploy:
  provider: releases
  api_key: $GITHUB_OAUTH_TOKEN
  overwrite: false
  file_glob: true
  file:
    - target/release/alias_*
  skip_cleanup: true
  draft: true
  on:
    tags: true
